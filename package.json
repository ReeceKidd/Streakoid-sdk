{
  "name": "streakoid",
  "version": "1.0.0",
  "description": "Streakoid SDK",
  "main": "lib/index.js",
  "types": "lib",
  "scripts": {
    "build": "tsc -p .",
    "unit-tests": "jest --config=./jest.unit.config.js --coverage",
    "lint": "tslint --project tsconfig.json",
    "intergration-tests": "jest --config=./jest.intergration.config.js --runInBand --forceExit",
    "test": "npm run unit-tests && npm run intergration-tests"
  },
  "author": "reecekidd",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "husky": "^3.0.5",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/mongoose": "^5.5.17",
    "axios": "^0.19.0",
    "dotenv": "^8.1.0",
    "mongoose": "^5.7.0"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm run test"
    }
  }
}