enum RouterCategories {
    agendaJobs = 'agenda-jobs',
    feedbacks = 'feedbacks',
    users = 'users',
    soloStreaks = 'solo-streaks',
    teamStreaks = 'team-streaks',
    completeSoloStreakTasks = 'complete-solo-streak-tasks',
    incompleteSoloStreakTasks = 'incomplete-solo-streak-tasks',
    completeTeamMemberStreakTasks = 'complete-group-member-streak-tasks',
    incompleteTeamMemberStreakTasks = 'incomplete-group-member-streak-tasks',
    completeTeamStreakTasks = 'complete-team-streak-tasks',
    test = 'test',
    stripe = 'stripe',
    friends = 'friends',
    streakTrackingEvents = 'streak-tracking-events',
    teamMemberStreaks = 'group-member-streaks',
    friendRequests = 'friend-requests',
    dailyJobs = 'daily-jobs',
}

export default RouterCategories;
