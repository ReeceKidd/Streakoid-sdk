enum RouterCategories {
    agendaJobs = 'agenda-jobs',
    feedbacks = 'feedbacks',
    users = 'users',
    user = 'user',
    soloStreaks = 'solo-streaks',
    teamStreaks = 'team-streaks',
    streakRecommendations = 'streak-recommendations',
    completeSoloStreakTasks = 'complete-solo-streak-tasks',
    incompleteSoloStreakTasks = 'incomplete-solo-streak-tasks',
    completeTeamMemberStreakTasks = 'complete-team-member-streak-tasks',
    incompleteTeamMemberStreakTasks = 'incomplete-team-member-streak-tasks',
    completeTeamStreaks = 'complete-team-streak-tasks',
    incompleteTeamStreaks = 'incomplete-team-streaks',
    completeChallengeStreakTasks = 'complete-challenge-streak-tasks',
    incompleteChallengeStreakTasks = 'incomplete-challenge-streak-tasks',
    test = 'test',
    stripe = 'stripe',
    followers = 'followers',
    streakTrackingEvents = 'streak-tracking-events',
    teamMemberStreaks = 'team-member-streaks',
    friendRequests = 'follower-requests',
    dailyJobs = 'daily-jobs',
    emails = 'emails',
    profileImages = 'profile-images',
    challenges = 'challenges',
    challengeStreaks = 'challenge-streaks',
    notes = 'notes',
    activityFeedItems = 'activity-feed-items',
    databaseStats = 'database-stats',
    following = 'following',
    achievements = 'achievements',
}

export default RouterCategories;
